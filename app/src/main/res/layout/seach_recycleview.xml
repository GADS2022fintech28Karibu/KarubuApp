<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="190dp"
        android:layout_height="150dp"
        android:elevation="20dp"
        app:cardCornerRadius="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/ivSearchItem"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/holder_item" />

    </androidx.cardview.widget.CardView>

    <Button
        android:id="@+id/btnAddToCart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#ffcc99"
        android:text="Add to cart"
        android:layout_marginTop="10dp"
        android:layout_marginStart="10dp"
        app:icon="@drawable/ic_baseline_add_shopping_cart"
        app:layout_constraintStart_toEndOf="@+id/cardView"
        app:layout_constraintTop_toBottomOf="@+id/tvSearchItemPrice" />

    <TextView
        android:id="@+id/tvsearch_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Burger"
        android:fontFamily="@font/semi_bold"
        android:textSize="20sp"
        android:padding="5dp"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toEndOf="@+id/cardView"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvSearchItemPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Kes 900"
        android:layout_marginTop="10dp"
        android:fontFamily="@font/semi_bold"
        android:textSize="20sp"
        android:padding="5dp"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toEndOf="@+id/cardView"
        app:layout_constraintTop_toBottomOf="@+id/tvsearch_item_name" />

</androidx.constraintlayout.widget.ConstraintLayout>